{
    "extends": "astro/tsconfigs/strict",
    "exclude": ["dist", "prototype"],
    "include": ["src", "../server/src/payload-types.ts"],
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "@payload-types": ["../server/src/payload-types"]
        }
    }
}
